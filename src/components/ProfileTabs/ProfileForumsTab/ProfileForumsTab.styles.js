import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        backgroundColor: '#f8f8f8',
    },
    forum: {
        padding: 16,
        marginBottom: 12,
        backgroundColor: '#fff',
        borderRadius: 8,
    },
    forumName: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 4,
    },
    forumDescription: {
        fontSize: 14,
        color: '#666',
    },
    noForumsText: {
        fontSize: 16,
        color: '#aaa',
        textAlign: 'center',
        marginTop: 20,
    },
});
